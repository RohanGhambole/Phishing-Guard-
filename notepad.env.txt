DATABASE_URL=postgresql://user:pass@127.0.0.1:5432/phishdb
REDIS_URL=redis://localhost:6379

# Optional — for AI scoring (skip if not using OpenAI)
OPENAI_API_KEY=sk-...

# Optional — for sending takedown emails
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=you@gmail.com
SMTP_PASS=your-app-password